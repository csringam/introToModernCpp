#include <iostream>
#include <vector>
#include "recurSection.h"

const void printVector(const std::vector<int>& vec) {
	for (const auto& elem : vec) {
		std::cout << elem << " ";
	}
	return;
}

int main() {

	return 0;
}